/** Default scroll step for keyboard navigation (px) */
export const DEFAULT_KEYBOARD_SCROLL_STEP = 24;

/** Default row height for the data grid (px) */
export const DEFAULT_ROW_HEIGHT = 32;

/** Default column header height for the data grid (px) */
export const DEFAULT_COLUMN_HEADER_HEIGHT = 32;
